{
    "$ref": "#/definitions/ICSSObserve<TElement>",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {
        "EventTarget": {
            "description": "EventTarget is a DOM interface implemented by objects that can receive events and may have listeners for them.",
            "title": "EventTarget",
            "type": "object"
        },
        "ICSSObserve<TElement>": {
            "properties": {
                "clone": {
                    "description": "Clone template inside when css match is found.",
                    "title": "clone",
                    "type": "boolean"
                },
                "customStyles": {
                    "description": "Insert some associated needed styles.",
                    "title": "customStyles",
                    "type": "string"
                },
                "latestMatch": {
                    "$ref": "#/definitions/EventTarget",
                    "description": "Latest Element matching css selector (and within the element specified by within-closest)",
                    "title": "latestMatch"
                },
                "observe": {
                    "description": "This attribute/property must be present/true for anything to happen.",
                    "title": "observe",
                    "type": "boolean"
                },
                "selector": {
                    "description": "CSS selector to monitor for.",
                    "title": "selector",
                    "type": "string"
                },
                "sym": {
                    "$ref": "#/definitions/Symbol",
                    "title": "sym"
                },
                "withinClosest": {
                    "description": "Matching elements must fall within the closest ancestor matching this css expression.",
                    "items": {
                        "attr": "within-closest"
                    },
                    "title": "withinClosest",
                    "type": "string"
                }
            },
            "title": "ICSSObserve<TElement>",
            "type": "object"
        },
        "Symbol": {
            "properties": {
                "__@toStringTag": {
                    "title": "__@toStringTag",
                    "type": "string"
                }
            },
            "title": "Symbol",
            "type": "object"
        }
    }
}

